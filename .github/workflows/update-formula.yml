name: Update Atopile Formula

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to update to (e.g. 0.1.0)'
        required: false
        type: string

  schedule:
    - cron: "25 16 * * *"

jobs:
  update-formula:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5
        with:
          version: "0.6.4"

      - name: Update formula
        id: update-formula
        env:
          TARGET_VERSION: ${{ inputs.version }}
        run: uv run scripts/update_atopile_formula.py >> "$GITHUB_OUTPUT"

      - name: Create branch and commit
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git checkout -b "v${{ steps.update-formula.outputs.version }}"
          git add Formula/atopile.rb
          git commit -m "atopile ${{ steps.update-formula.outputs.version }}"
          git push origin "v${{ steps.update-formula.outputs.version }}"

      - name: Create pull request
        env:
          GH_TOKEN: ${{ secrets.PAT }}
        run: |
          gh pr create \
            --title "atopile ${{ steps.update-formula.outputs.version }}" \
            --body "This PR updates the \`atopile\` formula to version ${{ steps.update-formula.outputs.version }}.

            This update was automatically triggered by the release workflow." \
            --base main \
            --head "v${{ steps.update-formula.outputs.version }}"
